import {BernardClient} from "../../Librairie";
import {Modal} from "discord.js";

export default async function (client: BernardClient, interaction: Modal) {};

export const modal = {
    data: {
        name: "nameModal",
    }
}